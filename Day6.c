#include <stdio.h>
int main() {
 
 // Input values
 float P = 1000, R = 20, T = 10;
 // Calculate simple interest
 float SI = (P * T * R) / 100;
 // Print Simple Interest
 printf("Simple Interest = %f\n", SI);
 return 0;
}
